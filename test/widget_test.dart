import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('VeryMed smoke test', (WidgetTester tester) async {
    // Tests complets nécessitent Hive + Firebase initialisés
    expect(true, isTrue);
  });
}
